CREATE TABLE Phones (
                         PhonesID int UNIQUE AUTO_INCREMENT NOT NULL,
                         Model_Name varchar(255) NOT NULL,
                         Price varchar(255) NOT NULL
                         );
